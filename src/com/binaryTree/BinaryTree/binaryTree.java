package com.binaryTree.BinaryTree;

import com.binaryTree.Node;

public class binaryTree {
    public Node root;
    binaryTree(int data){
        root = new Node(data);
    }
    public binaryTree(){
        root = null;
    }
}
